import { MapFeatureFlags } from "../../flags";
import { MapFeature } from '../feature';

export function MVTSource2MapFeatures(tileSourceUrl: String, abortController: AbortController, featureFlags: MapFeatureFlags): MapFeature[] {
  return [];
}