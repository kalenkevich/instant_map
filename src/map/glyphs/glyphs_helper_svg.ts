import { SvgGlyphsTextureAtlasConfig, TextureAtlasState } from './glyphs_config';

export async function getGlyphTextureAtlasFromSvgConfig(
  config: SvgGlyphsTextureAtlasConfig
): Promise<TextureAtlasState> {
  throw new Error('SvgGlyphsTextureAtlasConfig is not supported :(.');
}
